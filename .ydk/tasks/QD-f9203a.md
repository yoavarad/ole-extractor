---
id: QD-f9203a
title: Add graphify config exclusion for third_party/npoi submodule -- graphify update hangs (tens of minutes, non-terminating in practice) indexing vendored NPOI scratchpad/legacy .NET Framework 2.0/4.0 projects once the submodule is initialized in a worktree. graphify-out/graph.json currently has zero third_party nodes (never successfully indexed). Add an exclusion rule so graphify update skips third_party/ entirely.
status: in-progress
type: quickdev
---

Add graphify config exclusion for third_party/npoi submodule -- graphify update hangs (tens of minutes, non-terminating in practice) indexing vendored NPOI scratchpad/legacy .NET Framework 2.0/4.0 projects once the submodule is initialized in a worktree. graphify-out/graph.json currently has zero third_party nodes (never successfully indexed). Add an exclusion rule so graphify update skips third_party/ entirely.
